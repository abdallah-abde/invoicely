export const dynamic = "force-dynamic";

import { RequestPasswordForm } from "@/features/auth/components/request-password-form";

export default async function RequestPasswordPage() {
  return <RequestPasswordForm />;
}
